# OpenCV-Maze-Solver
A Python-based maze solver that processes maze images using OpenCV, finds the shortest path using BFS, generates solved maze images, and computes a final password from path lengths.
